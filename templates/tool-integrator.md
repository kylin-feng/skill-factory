# 工具集成 Skill 模板（Tool Integrator）

构建时读取此模板，按 `[占位符]` 填充。

---

```markdown
---
name: [tool-name]-assistant
description: |
  [工具名]操作助手。通过自然语言执行[工具名]的[主要操作列表]。
  触发词：「[工具名]」「帮我在[工具名]上...」「[操作动词][工具对象]」
  场景：[描述用户什么时候需要通过 Claude 操作这个工具]
  需要：[认证配置/环境变量/工具安装] 已就绪
---

# [工具名]助手

> [这个工具集成解决的核心痛点]

## 前置要求

**必须配置**（首次使用时检查）：
- [ ] [环境变量/API Key]：`[变量名]`
- [ ] [工具安装]：`[安装命令]`
- [ ] [权限要求]：[需要什么权限]

**检查命令**：
```bash
[验证配置是否正确的命令]
```

## 支持的操作

### 操作1: [操作名]

**触发**：用户说「[自然语言描述]」
**参数**：
- [参数1]（必填）：[说明]
- [参数2]（可选，默认[默认值]）：[说明]

**执行**：
```bash
[具体命令或API调用]
```

**确认机制**：
- 危险操作（删除/修改）：执行前展示操作摘要，等待用户确认
- 只读操作：直接执行
- 批量操作：告知影响范围，等待确认

**返回格式**：
```
✅ [操作名] 成功
[操作结果摘要]
[关键信息，如创建的ID、URL等]
```

### 操作2: [操作名]
[同上格式]

## 自然语言解析

用户可能用多种方式表达同一意图，统一处理：

| 用户说的 | 识别为操作 | 提取参数 |
|---------|----------|---------|
| [表达1] | [操作名] | [参数提取规则] |
| [表达2] | [操作名] | [参数提取规则] |

**歧义处理**：当意图不明确时，展示可能的操作选项，让用户确认。

## 错误处理

| 错误类型 | 原因 | 用户友好的提示 |
|---------|------|--------------|
| 认证失败 | API Key 无效或过期 | [如何重新配置] |
| 权限不足 | 缺少操作权限 | [如何申请权限] |
| 资源不存在 | ID/名称错误 | [如何查找正确的ID] |
| 频率限制 | 请求太频繁 | [等待时间和重试建议] |
| 网络错误 | 连接失败 | [排查步骤] |

## 安全守则

**绝不自动执行的操作**（必须用户明确确认）：
- 删除操作
- 批量修改
- 权限变更
- 发送通知/消息给他人

**敏感信息处理**：
- 不在日志/输出中显示 API Key
- 不将认证信息写入文件

## 诚实边界

- 只支持[列出支持的操作]，不支持[列出不支持的]
- API 限制：[速率限制/配额]
- [工具名]的[具体功能]需要[付费计划/特定权限]

---

> 本 Skill 由 [Skill工厂](https://github.com/your-repo/skill-factory) 生成
```
